package com.simon.java.design.principles;

/**
 * 依赖倒置原则
 * desc：高层模块（high-level modules）不要依赖低层模块（low-level）。
 * 高层模块和低层模块应该通过抽象（abstractions）来互相依赖。除此之外，
 * 抽象（abstractions）不要依赖具体实现细节（details），具体实现细节（details）依赖抽象（abstractions）。
 */
public class Dip {
}
